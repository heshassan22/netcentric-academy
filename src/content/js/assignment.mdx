export const meta = {
  order: 99,
  num: '★',
  title: 'Assignment: Build a Team Directory',
  topics: 'capstone · fetch → transform → render → filter · functional'
};

Build one component that ties the whole track together: a **Team Directory** that loads people from a
mock API, shapes the data, renders cards, and filters them live — all in the functional style. Build it
in `ui.frontend`. Budget **~2 hours**.

<Callout type="note">This combines the lessons — it is **not** a copy of any one demo. Data comes from the free **JSONPlaceholder** API: `https://jsonplaceholder.typicode.com/users`.</Callout>

The markup you enhance:

```html
<div class="directory" data-endpoint="https://jsonplaceholder.typicode.com/users">
  <input class="directory__search" type="search" placeholder="Search names…">
  <p class="directory__status"></p>
  <ul class="directory__list"></ul>
</div>
```

## Build it

- [ ] **Structure** — a functional component initialised on the root; keep its state in closures, scoped to the root
- [ ] **Load** — fetch the people from the API, with a loading state and an error state
- [ ] **Shape** — turn the raw response into just what a card needs, as a pure step
- [ ] **Render** — build the cards from the shaped data and put them on the page
- [ ] **Filter** — as the user types, show only the matching cards and keep a running count

How you split the functions and name things is up to you — aim for clean, readable modules.

<Callout type="do">Done when it loads from the API (with loading + error states), renders the cards, and filters live with a running count. Compare with the [answer](assignment-answer).</Callout>
